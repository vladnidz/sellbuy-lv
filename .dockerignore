# Build / runtime artifacts
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.next
out
build
coverage
*.tsbuildinfo
next-env.d.ts

# Local dev scripts
check_users.ts
create_demo_user.ts

# Secrets & local env (never bake credentials into the image)
.env
.env.local
.env.*.local

# IDE / OS
.DS_Store
.idea
.vscode
*.pem

# VCS
.git

# NOTE: docker-entrypoint.js must NOT be ignored here — the builder stage
# COPYs it into the image.
Dockerfile
docker-compose.yml

# Docs (not needed in image)
DEPLOYMENT_READINESS.md
DESIGN_AUDIT.md
ROADMAP.md
AGENTS.md
CLAUDE.md

# Tests (build only needs source + config)
__tests__
jest.config.mjs
jest.setup.ts

# Skills / harness artifacts
skills-lock.json
